/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // Tailwind v3 uses the 'tailwindcss' plugin name
    tailwindcss: {},
    autoprefixer: {},
  },
}

export default config