# Copy this file to your server and rename as /etc/script-to-video/worker.env
# Then edit the values below with your real secrets.

# Supabase (must match the project your API uses)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=REPLACE_ME

# Azure Blob Storage (the account where rendered videos will be uploaded)
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=YOUR_ACCOUNT;AccountKey=REPLACE_ME;EndpointSuffix=core.windows.net
AZURE_STORAGE_ACCOUNT_NAME=YOUR_ACCOUNT
AZURE_STORAGE_ACCOUNT_KEY=REPLACE_ME

# Worker mode
# false = polling Supabase for pending jobs (no Redis required)
# true  = BullMQ via Redis (also set REDIS_URL)
RENDER_QUEUE_ENABLED=false
# REDIS_URL=rediss://:<password>@<host>:6380

# Optional working directory for temporary files
WORK_DIR=/var/tmp

# Optional: explicitly set Chromium/Chrome path if auto-detection fails (common under systemd)
#REMOTION_BROWSER_EXECUTABLE=/snap/bin/chromium
#PUPPETEER_EXECUTABLE_PATH=/snap/bin/chromium
